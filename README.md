# dotfiles

My collection of configs and general dotfiles

## Programs

Edit `arch-repo.txt` and `aur.txt` to chose which programs to install.

To install: `sudo pacman -S $(cat arch-repo.txt)`
And afterwards: `yay -S $(cat aur.txt)`
